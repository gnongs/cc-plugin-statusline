---
description: Remove the claude-statusline from settings
allowed-tools: [Read, Edit]
---

# Uninstall Claude Statusline

Remove the statusline configuration from Claude Code settings.

## Instructions

1. Read `~/.claude/settings.json`
2. Remove the `statusLine` key from the JSON
3. Save the file
4. Confirm to the user that the statusline has been removed
